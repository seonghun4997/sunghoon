echo "[커밋 전 검사] 빌드가 통과하는지 확인합니다..."

npm run build || {
  echo ""
  echo "=================================================="
  echo " 커밋이 취소됐습니다. 빌드가 실패했습니다."
  echo " 위에 나온 파일과 줄 번호를 고친 뒤 다시 커밋하세요."
  echo "=================================================="
  exit 1
}

echo "[커밋 전 검사] 통과. 커밋을 진행합니다."